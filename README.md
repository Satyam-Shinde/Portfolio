# Full-Stack Portfolio (React + Express + MongoDB)

## Folder Structure

- `client/` – Frontend (React)
- `server/` – Backend (Node + Express + MongoDB)

## Setup

### Frontend

```bash
cd client
npm install
npm start
```

### Backend

```bash
cd server
npm install
node index.js
```
